# Standalone workspace: the crate only builds on Windows, so it stays out of the
# root package's build.
[workspace]

[package]
name = "window-warp"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive"] }
scap = "0.0.8"
windows = { version = "0.62", features = ["Win32_Foundation", "Win32_Graphics_Direct3D", "Win32_Graphics_Direct3D11", "Win32_Graphics_Direct3D_Fxc", "Win32_Graphics_Dxgi", "Win32_Graphics_Dxgi_Common", "Win32_Graphics_Gdi", "Win32_UI_WindowsAndMessaging", "Win32_UI_HiDpi", "Win32_UI_Input_KeyboardAndMouse", "Win32_System_LibraryLoader", "Win32_System_Threading"] }

[target."cfg(windows)".dependencies]
# scap 0.0.8 depends on windows-capture "1.3.6" but does not build against 1.5.
windows-capture = "=1.4.4"
